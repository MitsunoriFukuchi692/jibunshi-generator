services:
  - type: web
    name: jibunshi-generator-backend
    root: backend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production